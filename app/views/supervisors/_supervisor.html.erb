<div id="<%= dom_id supervisor %>">
 <tr>
      <td><%= supervisor.name %></td>
      <td><%= supervisor.surname %></td>
      <td><%= supervisor.dni %></td>
      <td><%= supervisor.email %></td>
      <%#%<td>
          <% if supervisor.habilitado %>
     <%#%       HABILITADO
          <% else %>
      <%#%      NO HABILITADO
          <% end %>
     <%#%   </td>
    <%#%  <td>
          <% if supervisor.borrado %>
       <%#%     HABILITADO
      <%#%    <% else %>
        <%#%    NO HABILITADO
          <% end %>
     <%#%   </td> %>
      <td> 
        <%= link_to "Detalles", supervisor_path(supervisor), class:"btn btn-dark" %>
        <%= link_to "Editar", edit_supervisor_path(supervisor), class:"btn btn-dark" %>
      </td>
    </tr>
</div>
